# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Firebase Configuration (Development Mode)
# For development, Firebase will work without credentials (console logging)
# For production, set FIREBASE_SERVICE_ACCOUNT_KEY with your service account JSON
FIREBASE_PROJECT_ID=rideshare-hub-dev

# Legacy PostgreSQL (No longer needed)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/rideshare

# Session Configuration
# Generate a secure random string for production
SESSION_SECRET=dev-secret-key-change-in-production-12345

# Application Configuration
NODE_ENV=development
PORT=5000
